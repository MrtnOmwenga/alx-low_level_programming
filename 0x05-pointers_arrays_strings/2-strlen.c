#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 *_strlen- Returns length of string
 *
 *@s: String to be checked
 *
 *Return: int
 */

int _strlen(char *s)
{
size_t len = strlen(s);

return (len);
}
