#include <stdio.h>
#include <ctype.h>

/**
 * _isupper: Checks whether character passed is even
 *
 * Return: int
 */

int _isupper(int c)
{
if (isupper(c) == 1)
{
return (1);
}
else
{
return (0);
}
}
